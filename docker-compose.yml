version: "3.9"

services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - ig_data:/data
    env_file:
      - .env
    environment:
      - DB_PATH=/data/messages.db

volumes:
  ig_data:
